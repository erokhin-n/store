export enum Route {
    Login = "login",
    DevicePage = "device_page",
    Shop = "/"
}

export enum ServerQuery {
    devices = '/api/device',
    registration = '/api/user/registration',
    login = '/api/user/login'
}
